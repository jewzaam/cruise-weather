// Generated By: Claude Code (claude-sonnet-4-6)
package org.jewzaam.cruiseweather.data.local.entity

import androidx.room.Entity
import androidx.room.ForeignKey
import androidx.room.Index
import androidx.room.PrimaryKey
import java.time.LocalDate
import java.util.UUID

@Entity(
    tableName = "ports_of_call",
    foreignKeys = [
        ForeignKey(
            entity = Cruise::class,
            parentColumns = ["id"],
            childColumns = ["cruiseId"],
            onDelete = ForeignKey.CASCADE,
        ),
    ],
    indices = [Index("cruiseId")],
)
data class PortOfCall(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    /** Stable client-side identity — unique even before persistence. */
    val localId: String = UUID.randomUUID().toString(),
    val cruiseId: Long,
    val portName: String,
    val date: LocalDate,
    // DEPARTURE and RETURN are auto-created when a cruise is saved.
    // They appear read-only in the port list UI.
    val type: PortType = PortType.PORT_OF_CALL,
    val latitude: Double? = null,
    val longitude: Double? = null,
    val resolvedDisplayName: String? = null,
    val sortOrder: Int = 0,
)
