// Generated By: Claude Code (claude-sonnet-4-6)
package org.jewzaam.cruiseweather.data.local

import androidx.room.Dao
import androidx.room.Delete
import androidx.room.Insert
import androidx.room.Query
import androidx.room.Update
import kotlinx.coroutines.flow.Flow
import org.jewzaam.cruiseweather.data.local.entity.PortOfCall

@Dao
interface PortOfCallDao {
    @Query("SELECT * FROM ports_of_call WHERE cruiseId = :cruiseId ORDER BY date ASC, sortOrder ASC")
    fun getPortsForCruise(cruiseId: Long): Flow<List<PortOfCall>>

    @Query("SELECT * FROM ports_of_call WHERE id = :portId")
    suspend fun getPortById(portId: Long): PortOfCall?

    @Insert
    suspend fun insert(port: PortOfCall): Long

    @Insert
    suspend fun insertAll(ports: List<PortOfCall>)

    @Update
    suspend fun update(port: PortOfCall)

    @Delete
    suspend fun delete(port: PortOfCall)

    @Query("DELETE FROM ports_of_call WHERE id = :portId")
    suspend fun deleteById(portId: Long)

    @Query("DELETE FROM ports_of_call WHERE cruiseId = :cruiseId AND type IN ('DEPARTURE', 'RETURN')")
    suspend fun deleteDepartureReturnPorts(cruiseId: Long)
}
